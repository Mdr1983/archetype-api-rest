spring:
  application:
    name: archetype-logistic-api
  cloud:
    config:
      failFast: true
      enabled: true
      name: ${spring.application.name}
      profile: ${SHORT_ENV}
      uri: http://localhost:8889
      server:
        git:
          search-paths: config
logging:
  #config: ${SCCS}/sccs/${spring.application.name}/${SHORT_ENV}/master/base_logback.xml
  config: classpath:logback_conf.xml

